export default function NavigationBar() {
    return <nav className="fixed bottom-0 left-0 right-0 flex justify-center items-center gap-10">
        <button>🗓️ Calendario</button>
        <button>⚙️ Impostazioni</button>
    </nav>
}